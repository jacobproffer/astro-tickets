---
import Container from "./Container.astro";

interface Props {
  headline: string;
}

const { headline } = Astro.props;
---

<section class="relative">
  <Container>
    {headline && <h2 class="text-6xl">{headline}</h2>}
    <slot />
  </Container>
</section>
