---
interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className, ...rest } = Astro.props;
---

<div class:list={["flex flex-col gap-y-4 px-4", className]} {...rest}>
  <slot />
</div>
