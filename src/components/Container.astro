---
interface Props {
  as?: string;
  class?: string;
  [key: string]: any;
}

const { as: Tag = "div", class: className, ...rest } = Astro.props;
---

<Tag class:list={["flex flex-col gap-y-4 px-4", className]} {...rest}>
  <slot />
</Tag>
