---
interface Props {
  page: {
    url: {
      prev?: string;
      next?: string;
    };
    currentPage: number;
    lastPage: number;
    start: number;
    end: number;
    total: number;
  };
}

const { page } = Astro.props;
---

<nav
  aria-label="Pagination Navigation"
  class="flex items-center justify-between border-t border-black-700 pt-6"
>
  <div class="flex flex-1 justify-between sm:hidden">
    {
      page.url.prev && (
        <a
          href={page.url.prev}
          class="relative inline-flex items-center rounded-md border border-solid px-4 py-2 font-medium hover:bg-gray-700"
        >
          Previous
        </a>
      )
    }
    {
      page.url.next && (
        <a
          href={page.url.next}
          class="relative ml-3 inline-flex items-center rounded-md border border-solid px-4 py-2 font-medium hover:bg-gray-700"
        >
          Next
        </a>
      )
    }
  </div>

  <div class="hidden sm:flex sm:flex-1 sm:items-center sm:justify-between">
    <div>
      <p>
        Showing <span class="font-medium">{page.start + 1}</span> to{" "}
        <span class="font-medium">{page.end + 1}</span> of{" "}
        <span class="font-medium">{page.total}</span> tickets
      </p>
    </div>

    <div class="flex items-center space-x-2">
      {
        page.url.prev && (
          <a
            href={page.url.prev}
            class="relative inline-flex items-center rounded-l-md border border-solid px-2 py-2 font-medium hover:bg-gray-700 focus:z-20"
          >
            <span class="sr-only">Previous</span>←
          </a>
        )
      }

      <!-- Current page indicator -->
      <span
        class="relative inline-flex items-center px-4 py-2 font-medium border border-solid z-10 focus:z-20"
      >
        {page.currentPage}
      </span>

      {
        page.lastPage > 1 && (
          <span class="relative inline-flex items-center px-2 py-2 font-medium">
            of {page.lastPage}
          </span>
        )
      }

      {
        page.url.next && (
          <a
            href={page.url.next}
            class="relative inline-flex items-center rounded-r-md border border-solid px-2 py-2 hover:bg-gray-700 focus:z-20"
          >
            <span class="sr-only">Next</span>→
          </a>
        )
      }
    </div>
  </div>
</nav>
