---
interface Props {
  href: string;
  isActive?: boolean;
  class?: string;
}

const { href, isActive, class: className } = Astro.props;
---

<a
  href={href}
  class:list={[
    "block text-black py-3 px-4 border-l border-dashed border-black text-center transition-colors hover:bg-gray-100 focus:outline focus:outline-black",
    isActive && "border-black bg-green",
    className,
  ]}
  aria-current={isActive ? "page" : undefined}
>
  <slot />
</a>
